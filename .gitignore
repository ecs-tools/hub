# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Installers and binaries
*.exe

# Environment variables — never commit these
.env
.env.*

# Local agent / editor state
.claude/

# Client data — contains real client names + billing (PII). Never commit.
# The app reads billing from the API at runtime, not this file.
public/billing_data.json

# Local-only data that must never ship with the site (PII)
_local/
